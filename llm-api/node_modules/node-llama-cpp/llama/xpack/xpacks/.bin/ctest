#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

exec "D:/projects/auto-agent/llm-api/node_modules/node-llama-cpp/llama/xpack/store/@xpack-dev-tools/cmake/3.27.9-1.2/.content/bin/ctest.exe"   "$@"
